import Home from './Home'
import CreatePost from './CreatePost'

// we can also export two things as one object 

export {
    Home,
    CreatePost 
}

//its like a package , so that we can simply use ./pages to import these stuff , not the exact file location 